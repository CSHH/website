<div id="activity-switch" n:if="$canAccess">
    {if $inactiveOnly}
        <a n:href="default, inactiveOnly => 'no'">Zobrazit vše</a>
    {else}
        <a n:href="default, inactiveOnly => 'yes'">Zobrazit pouze neschválené</a>
    {/if}
</div>

<div>

    <div>
        <label>
            {_}locale.form.tag{/_}
        </label>
    </div>

    <div>
        <select name="tagId">
            <option n:foreach="$tags as $id => $name" value="{$id}">{$name}</option>
        </select>
    </div>

</div>

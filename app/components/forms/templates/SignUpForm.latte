{form form}

    {foreach $form->errors as $error}
        <div>{$error}</div>
    {/foreach}

    <div>
        {label username /}
        {input username}
    </div>

    <div>
        {label email /}
        {input email}
    </div>

    <div>
        {label password /}
        {input password}
    </div>

    <div>
        {label password_confirm /}
        {input password_confirm}
    </div>

    {input __anti}

    <div>
        {input submit}
    </div>

{/form}

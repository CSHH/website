{block title} {_}locale.sections.articles{/_} {if $tag} - {$tag->name}{/if} {/block}

{block content}

{foreach $articles as $article}

    <div>
        <h2><a n:href="detail, tagSlug => $article->tag->slug, slug => $article->slug">{$article->id}. {$article->name}</a></h2>
        <p>{$article->perex}</p>
    </div>

{/foreach}

{control vp}
